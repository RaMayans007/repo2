name: Trigger Repo1 Webhook
on:
  push:
    branches:
      - main  # Adjust if needed

jobs:
  send-webhook:
    runs-on: ubuntu-latest
    steps:
      - name: Send Dispatch Event to Repo1Add commentMore actions
        run: |
          curl -X POST "https://api.github.com/repos/RaMayans007/repo1/dispatches" \
          -H "Authorization: token ${{ secrets.WEBHOOK_PAT }}" \
          -H "Accept: application/vnd.github.v3+json" \
          -H "Content-Type: application/json" \
          -H "X-Hub-Signature-256: sha256=$(echo -n ${{ secrets.WEBHOOK_SECRET }} | openssl dgst -sha256 -hmac ${{ secrets.WEBHOOK_SECRET }} | cut -d " " -f2)" \
          -d '{"event_type": "trigger-script"}'
